<%= @comment.comment %>

<% if @comment.user.id == current_user.id %>
<td><%= link_to "Destroy",book_book_comment_path, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger destroy_book_book_comment_#{@comment.id}" %></td>
<% end %>